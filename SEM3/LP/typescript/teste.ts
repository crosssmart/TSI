let idade: number = 25;

let nome: string;
nome = 'Aderbal';

let ativo: boolean = true;

let notas: number[] = [1,2,3,4];

let pessoa: [string, number] =["joao", 20];

let valor: any = "Pode ser qualquer coisa";

// ou
let doistipos: number | string = 2

function soma(num1: number, num2: number): number {
  return num1 + num2;
}
